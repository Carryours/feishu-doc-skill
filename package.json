{
  "name": "feishu-doc-skill",
  "version": "0.1.0",
  "private": true,
  "description": "Feishu doc skill for Codex",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "check": "node --check scripts/feishu_oauth_server.js && node --check scripts/extract_feishu_doc_id.js && node --check scripts/read_feishu_doc.js && node --check scripts/read_feishu_url.js && node --check scripts/read_feishu_url_md.js && node --check scripts/insert_feishu_local_image.js",
    "test": "node --test tests/*.test.js"
  }
}
